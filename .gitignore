node_modules/

# Retired/superseded local working data (old download-tool test runs, a raw
# text backup) — never commit. The active book-input folder is
# automation/book-library/ (see automation/.gitignore), not this one.
_archive/
